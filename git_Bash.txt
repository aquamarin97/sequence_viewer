# Değişiklikleri görmek için
git status

# Tüm değişiklikleri eklemek için (.gitignore'dakiler hariç)
git add .

# Commit ve push
git commit -m "zoom out update"
git push origin master

git remote add origin https://github.com/aquamarin97/sequence_viewer